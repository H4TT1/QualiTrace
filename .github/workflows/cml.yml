name: CML Smoke Pipeline

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  smoke-train:
    name: CPU smoke training
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # FIX: Install system graphics dependencies required by node-canvas/CML tools
      - name: Install System Graphics Dependencies for CML Canvas
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev libpixman-1-dev

      - name: Set up CML
        uses: iterative/setup-cml@v2

      - name: Create synthetic MVTec smoke dataset
        run: python scripts/create_smoke_dataset.py

      - name: Check Python syntax
        run: python -m py_compile src/*.py src/models/*.py src/experiments/*.py

      - name: Run CPU PatchCore smoke pipeline
        env:
          PYTHONPATH: src
        run: python src/train.py --config config/ci.yaml

      - name: Plot PatchCore scores
        run: python scripts/plot_patchcore_scores.py

      - name: Build CML report
        run: |
          echo "## QualiTrace CML Smoke Pipeline" > report.md
          echo "" >> report.md
          echo "- CPU PatchCore smoke run completed." >> report.md
          echo "- Synthetic MVTec-style dataset: 2 train images, 2 test images." >> report.md
          echo "- Config: \`config/ci.yaml\`" >> report.md
          echo "" >> report.md
          cml publish .ci_outputs/patchcore_scores.png --md >> report.md

      - name: Comment CML report on PR
        if: github.event_name == 'pull_request'
        env:
          REPO_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: cml comment create report.md

      - name: Upload smoke artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cml-smoke-artifacts
          path: |
            report.md
            .ci_outputs/patchcore_scores.png
            .ci_outputs/models/data_validation_report.json
            .ci_outputs/models/patchcore_scores.json